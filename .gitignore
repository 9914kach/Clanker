# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.eggs/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo

# Cursor — local IDE/agent state (keep shared rules: .cursor/rules/)
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Other AI / agent local dirs (if tools create them in the repo)
.agent/
.claude/
.aider*
.specstory/

# Secrets (lägg aldrig nycklar i repot)
*.pem
.env.local
.env.*
!.env.example

# Pi-hole runtime data
etc-pihole/
etc-dnsmasq.d/

# Docker local overrides
docker-compose.override.yml

# Clanker runtime (Vite PID/log från clanker-run) — inte mappen scripts/ i repot
.clanker/

# Node / frontend
node_modules/
apps/discord-hub-web/dist/
